<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.altair.app">

    <!-- Ubicación -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Background location: NO lo pedimos al inicio, solo si el usuario activa medición en 2do plano -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Teléfono (para CellInfo) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Android 13+ para notificaciones del foreground service -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NemoBackPack"
        tools:targetApi="34">

        <!-- osmdroid -->
        <meta-data android:name="osmdroid.basePath" android:value="osmdroid" />
        <meta-data android:name="osmdroid.cachePath" android:value="osmdroid/cache" />

        <!-- LAUNCHER -->
        <activity
            android:name="com.altair.app.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.altair.app.OnboardingActivity" android:exported="false" />
        <activity android:name="com.altair.app.LoginActivity" android:exported="false" />

        <!-- ✅ ESTA ES TU APP PRINCIPAL LOGUEADA -->
        <activity android:name="com.altair.app.HomeActivity" android:exported="false" />

        <!-- Servicios -->
        <service
            android:name="com.altair.app.ForegroundMeasurementService"
            android:exported="false"
            android:foregroundServiceType="location|dataSync" />

        <service
            android:name="com.altair.app.LocalMeasurementService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>
